# Backend Environment Variables
PORT=5000

# Supabase Configuration
# Get these from: Supabase Dashboard → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# ⚠️ CRITICAL: Supabase Service Role Key (Secret)
# NEVER expose this in frontend code or commit to version control
# Bypasses Row Level Security (RLS) - has full database access
# Required for RLS security to work properly with backend operations
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT Authentication
# Generate a strong random string for production
JWT_SECRET=your-jwt-secret-key-change-in-production

# Blockchain Configuration (Local Development)
RPC_URL=http://127.0.0.1:8545

# PRIVATE_KEY: Get from Hardhat node terminal output (Account #0)
# Run 'npx hardhat node' to see available test accounts
# For production: use a secure private key
PRIVATE_KEY=your-hardhat-account-0-private-key-here

# Smart Contract Address
# Fill this after deploying contract with: npx hardhat run scripts/deploy.js
CONTRACT_ADDRESS=your-deployed-contract-address-here

